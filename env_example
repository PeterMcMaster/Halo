# Halo environment placeholders

# Toggle GPU acceleration (set to 1 if CUDA is available)
USE_CUDA=0

# Vector search + embedding settings
CLIP_MODEL=openai/clip-vit-base-patch32
BLIP_MODEL=Salesforce/blip-image-captioning-base
ENABLE_BLIP=1
CAPTION_WEIGHT=0.2
DEFAULT_TOP_K=12

# LLM provider configuration
# Valid values: openai, gemini, none
LLM_PROVIDER=gemini

# OpenAI (optional)
OPENAI_API_KEY=YOUR_OPENAI_KEY_HERE
OPENAI_MODEL=gpt-4o-mini

# Google Gemini (choose this if you only have a Gemini key)
GEMINI_API_KEY=YOUR_GEMINI_KEY_HERE
GEMINI_MODEL=gemini-2.5-flash